# Mini Course Management System

> **Team:** Kavy Khilrani · Ruchi Pawar · Aarjica Talati  
> **Subject:** Database Management Systems (DBMS) College Project  
> **Last Updated:** March 2026

A database-driven Java Swing application for managing academic activities — course registration, assignment submission, grading, and student–instructor interaction.

---

## 📋 Features

### 🎓 Student View
| Feature | Description |
|---|---|
| **My Courses** | View all enrolled courses with instructor and department info |
| **Assignments** | See all assignments across courses with deadlines and submission status (✅ / ❌) |
| **Submit Assignment** | Dropdown of pending assignments — select and click Submit |
| **My Grades** | View graded submissions with marks and instructor feedback |

### 👨‍🏫 Instructor View
| Feature | Description |
|---|---|
| **Manage Courses** | View all your courses with enrolled student count; add new courses (ID auto-generated) |
| **Manage Assignments** | View all assignments; create new ones by selecting course, entering title and deadline |
| **Grade Submissions** | View all student submissions; click a row to auto-fill the grading form; save or update grades with feedback |

### 🔀 Profile Switching
Switching between Student and Instructor views automatically updates the profile name in the top navigation bar:
- **Student View** → `Kavy Khilrani | Student`
- **Instructor View** → `Dr. Jashma Suresh | Instructor`

---

## 🛠️ Technology Stack

| Layer | Technology |
|---|---|
| Frontend (UI) | Java Swing (`JFrame`, `JTabbedPane`, `JTable`, `CardLayout`) |
| Backend Connectivity | JDBC (Java Database Connectivity) |
| Database (Runtime) | SQLite via `sqlite-jdbc` driver (zero installation, embedded) |
| Database (Report) | MySQL / Oracle — see `schema.sql` |
| Language | Java (JDK 8 or above) |

---

## 📂 Project Structure

```
Dbs/
├── src/
│   ├── MainDashboard.java      # Main window, sidebar, top nav, view switching
│   ├── StudentPanel.java       # All student tabs and logic
│   ├── InstructorPanel.java    # All instructor tabs and logic
│   └── DBConnection.java       # SQLite connection, schema creation, sample data
├── lib/                        # Auto-downloaded JDBC driver jars (by run.bat)
│   ├── sqlite-jdbc.jar
│   ├── slf4j-api.jar
│   └── slf4j-simple.jar
├── bin/                        # Compiled .class files (generated by run.bat)
├── .vscode/
│   └── settings.json           # Adds lib/*.jar to VS Code classpath
├── schema.sql                  # MySQL/Oracle DDL + DML scripts for your report
├── run.bat                     # One-click build and launch script (Windows)
└── README.md                   # This file
```

---

## 🚀 How to Run

### Prerequisites
- **Java JDK 8 or above** must be installed.
- Verify with: `java -version` in Command Prompt.
- An active internet connection is required **only on the first run** to download the JDBC driver.

### ▶️ Option 1 — Recommended (run.bat)
Double-click **`run.bat`** in the project folder.

It will automatically:
1. Download `sqlite-jdbc.jar` into `lib/` (first run only).
2. Create the `bin/` folder and compile all `.java` source files.
3. Launch the application window.

### ▶️ Option 2 — From VS Code
The `.vscode/settings.json` file is included so VS Code automatically adds `lib/*.jar` to the classpath.
> ⚠️ Make sure the jars exist in `lib/` first — run `run.bat` once to download them, then you can launch from VS Code.

---

## 🗄️ Database

On first launch, the app automatically creates `course_management.db` (SQLite) in the project root folder and populates it with the following sample data:

| Table | Sample Records |
|---|---|
| Student | Kavy Khilrani, Ruchi Pawar, Aarjica Talati |
| Instructor | Dr. Jashma Suresh (CS), Dr. Grace Hopper (Maths) |
| Course | Intro to Databases, Data Structures, Linear Algebra, Operating Systems |
| Assignment | 6 assignments across courses with deadlines |
| Submission | 4 sample submissions with graded results |
| Grade | 2 pre-graded submissions with feedback |

> To **reset the database**, simply delete `course_management.db` and relaunch the app.

---

## 📝 For Your Academic Report (`schema.sql`)

The `schema.sql` file contains the pure **MySQL / Oracle** compatible version of all SQL scripts used in this project:
- `CREATE TABLE` statements with `PRIMARY KEY`, `FOREIGN KEY`, and `CHECK` constraints
- `INSERT` dummy data statements
- PL/SQL `PROCEDURE`, `FUNCTION`, and `TRIGGER` examples (Oracle syntax, commented)

Copy and paste the contents into **MySQL Workbench** or **Oracle SQL Developer** for your final report submission.

---

## 🔄 Changelog

| Version | Changes |
|---|---|
| v1.0 | Initial project setup — basic Java Swing UI, SQLite DB, student and instructor panels |
| v1.1 | Added rich sample data (3 students, 4 courses, 6 assignments, grades); fixed button colours; renamed student profile to Kavy |
| v1.2 | Full instructor functionality — Add Course, Add Assignment, Grade/Update submissions with row-click auto-fill; profile switches on view change; `.vscode/settings.json` added so app works from both `run.bat` and VS Code |
| v1.3 | **Live table updates** — all tabs refresh from the DB the moment they are clicked (no restart needed); **Fixed invisible button text** on Windows (Metal LAF override); **Changed instructor name** to Dr. Jashma Suresh |
